{# new.html.twig #}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign up</title>
    <link href="https://fonts.googleapis.com/css?family=Poppins:100,100i,200,200i,300,300i,400,400i,500,500i,600,600i,700,700i,800,800i,900,900i&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('css/signup.css')}}">
</head>
<body>
    <section class="signup">
        <div class="container">
            <div class="signup-content">
                <div class="signup-form">
                    <h2 class="form-title">Edit My Profile</h2>
                    <form method="post" action="{{ path('app_users_new') }}" class="register-form" id="register-form">
                        {{ form_start(formedit) }}
                        <div class="form-group">
                            {{ form_widget(formedit.name, {'attr': {'placeholder': 'Your Name'}}) }}
                        </div>
                        <div class="form-group">
                            {{ form_widget(formedit.email, {'attr': {'placeholder': 'Your Email'}}) }}
                        </div>
                        <div class="form-group">
                            {{ form_widget(formedit.age, {'attr': {'placeholder': 'Your Age'}}) }}
                        </div>
                        <div class="form-group">
                            {{ form_widget(formedit.genre, {'attr': {'placeholder': 'Your Gender'}}) }}
                        </div>
                        <div class="form-group">
                            {{ form_widget(formedit.numTel, {'attr': {'placeholder': 'Your Phone Number'}}) }}
                        </div>
                        <div class="form-group">
                            {{ form_widget(formedit.adresse, {'attr': {'placeholder': 'Your Address'}}) }}
                        </div>
                        <div class="form-group">
                            {{ form_widget(formedit.password, {'attr': {'placeholder': 'Password'}}) }}
                        </div>
                        <div class="form-group form-button">
                            <input type="submit" name="signup" id="signup" class="form-submit" value="Save">
                        </div>
                        {{ form_end(formedit) }}
                    </form>
                </div>
                <div class="signup-image">
                    <figure><img src="{{ asset('images/signup.jpg')}}" alt="sign up image"></figure>
                    
            </div>
        </div>
    </section>
</body>
</html>
